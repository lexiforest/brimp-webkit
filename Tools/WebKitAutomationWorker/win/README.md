# Windows port

The future Windows worker belongs in this directory. It should implement the
same versioned framed-JSON protocol as the macOS worker while using the WebKit
Windows embedding and native input APIs appropriate to that port.
