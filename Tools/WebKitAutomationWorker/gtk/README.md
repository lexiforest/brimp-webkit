# GTK port

The future WebKitGTK worker belongs in this directory. It should implement the
same versioned framed-JSON protocol as the macOS worker using WebKitGTK,
GLib/GApplication, and GTK input and view APIs.
